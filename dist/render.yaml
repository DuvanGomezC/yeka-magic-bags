git status
git add .   
git commit -m "se paso el render a la carpeta dist que es donde esta el proyecto plano "
git push origin main





services:
  - type: web
    name: yeka-magic-bags-frontend
    env: static
    buildCommand: rm -rf node_modules package-lock.json && npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: 18
    rewrites:
      - source: /*
        destination: /index.html